"use client";

import { useMemo, useState } from "react";
import { ChevronLeft, ChevronRight, Eye, EyeOff, LoaderCircle, Sparkles } from "lucide-react";
import { Button } from "@/components/ui/button";
import { EmptyState } from "@/components/ui/empty-state";
import type { Course, Difficulty, StudyDocument } from "@/types/domain";

type Card = { id: string; question: string; answer: string; difficulty: Difficulty };
export function FlashcardGenerator({ courses, documents }: { courses: Course[]; documents: StudyDocument[] }) {
  const [courseId, setCourseId] = useState(""); const [documentId, setDocumentId] = useState(""); const [count, setCount] = useState("10"); const [difficulty, setDifficulty] = useState<Difficulty>("medium"); const [cards, setCards] = useState<Card[]>([]); const [index, setIndex] = useState(0); const [revealed, setRevealed] = useState(false); const [pending, setPending] = useState(false); const [error, setError] = useState<string | null>(null);
  const readyDocs = useMemo(() => documents.filter((document) => document.processing_status === "ready" && (!courseId || document.course_id === courseId)), [documents, courseId]);
  async function generate() { setPending(true); setError(null); const response = await fetch("/api/flashcards", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ courseId: courseId || undefined, documentId: documentId || undefined, count: Number(count), difficulty }) }); const body = await response.json().catch(() => ({})); if (!response.ok) setError(body.error ?? "Flashcards could not be created."); else { setCards(body.flashcards); setIndex(0); setRevealed(false); } setPending(false); }
  const card = cards[index];
  return <div className="grid gap-6 xl:grid-cols-[340px_minmax(0,1fr)]"><form onSubmit={(event) => { event.preventDefault(); void generate(); }} className="card h-fit space-y-4 p-5"><div><h2 className="font-semibold">Build a deck</h2><p className="mt-1 text-sm leading-6 text-slate-500">Generated cards are grounded in the selected material and saved to your workspace.</p></div><label><span className="label">Course</span><select className="field" value={courseId} onChange={(event) => { setCourseId(event.target.value); setDocumentId(""); }}><option value="">Any course</option>{courses.map((course) => <option key={course.id} value={course.id}>{course.course_code ?? course.name}</option>)}</select></label><label><span className="label">Document</span><select className="field" value={documentId} onChange={(event) => setDocumentId(event.target.value)}><option value="">All ready material</option>{readyDocs.map((document) => <option key={document.id} value={document.id}>{document.original_filename}</option>)}</select></label><div className="grid grid-cols-2 gap-3"><label><span className="label">Cards</span><select className="field" value={count} onChange={(event) => setCount(event.target.value)}><option value="5">5</option><option value="10">10</option><option value="15">15</option><option value="20">20</option></select></label><label><span className="label">Difficulty</span><select className="field" value={difficulty} onChange={(event) => setDifficulty(event.target.value as Difficulty)}><option value="easy">Easy</option><option value="medium">Medium</option><option value="hard">Hard</option></select></label></div>{error && <p className="rounded-lg bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<Button className="w-full" disabled={pending || (!courseId && !documentId)} type="submit">{pending ? <><LoaderCircle size={16} className="animate-spin" />Generating…</> : <><Sparkles size={16} />Generate flashcards</>}</Button></form><section>{card ? <div className="card min-h-96 overflow-hidden"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><span className="text-sm font-medium text-slate-600">Card {index + 1} of {cards.length}</span><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold capitalize text-slate-600">{card.difficulty}</span></div><button onClick={() => setRevealed((value) => !value)} className="flex min-h-64 w-full flex-col items-center justify-center px-8 text-center"><span className="text-xs font-semibold uppercase tracking-[.14em] text-emerald-700">{revealed ? "Answer" : "Question"}</span><p className="mt-5 max-w-2xl text-xl font-medium leading-8 text-slate-800">{revealed ? card.answer : card.question}</p><span className="mt-8 inline-flex items-center gap-2 text-sm font-medium text-slate-500">{revealed ? <EyeOff size={16} /> : <Eye size={16} />}{revealed ? "Hide answer" : "Reveal answer"}</span></button><div className="flex justify-between border-t border-slate-100 p-4"><Button variant="secondary" disabled={index === 0} onClick={() => { setIndex((value) => value - 1); setRevealed(false); }}><ChevronLeft size={16} />Previous</Button><Button variant="secondary" disabled={index === cards.length - 1} onClick={() => { setIndex((value) => value + 1); setRevealed(false); }}>Next<ChevronRight size={16} /></Button></div></div> : <EmptyState title="Create a focused review deck" copy="Choose a course or a processed document to turn the important concepts into usable flashcards." />}</section></div>;
}
