import Link from "next/link";
import { BookOpenCheck, CircleCheckBig, KeyRound, Server } from "lucide-react";

export const dynamic = "force-dynamic";

export default function SetupRequiredPage() {
  return <main className="grid min-h-screen place-items-center bg-[#f6f8f5] px-5 py-10"><section className="card w-full max-w-2xl p-6 shadow-sm sm:p-8"><Link href="/" className="flex items-center gap-2 text-lg font-semibold"><span className="grid size-8 place-items-center rounded-xl bg-emerald-600 text-white"><BookOpenCheck size={18} /></span>StudyOS</Link><p className="mt-8 text-sm font-medium text-amber-700">Configuration needed</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">The interface is ready. Connect its services to use the workspace.</h1><p className="mt-3 max-w-xl text-sm leading-6 text-slate-600">This deployment does not have its Supabase environment variables yet, so StudyOS cannot securely create accounts or access data. Add the variables below in your local <code>.env.local</code> or Vercel project settings, then redeploy.</p><ol className="mt-6 space-y-4"><li className="flex gap-3"><span className="grid size-8 shrink-0 place-items-center rounded-lg bg-emerald-50 text-emerald-700"><Server size={17} /></span><div><p className="font-medium">Connect Supabase</p><p className="mt-1 text-sm text-slate-600">Set <code>NEXT_PUBLIC_SUPABASE_URL</code> and <code>NEXT_PUBLIC_SUPABASE_ANON_KEY</code>, then apply the migrations in <code>supabase/migrations</code>.</p></div></li><li className="flex gap-3"><span className="grid size-8 shrink-0 place-items-center rounded-lg bg-emerald-50 text-emerald-700"><KeyRound size={17} /></span><div><p className="font-medium">Enable AI features</p><p className="mt-1 text-sm text-slate-600">Set the server-only <code>OPENAI_API_KEY</code>. The other variables and deployment instructions are documented in the README.</p></div></li><li className="flex gap-3"><span className="grid size-8 shrink-0 place-items-center rounded-lg bg-emerald-50 text-emerald-700"><CircleCheckBig size={17} /></span><div><p className="font-medium">Reload and create an account</p><p className="mt-1 text-sm text-slate-600">Once configured, return to signup to begin using the private workspace.</p></div></li></ol></section></main>;
}
